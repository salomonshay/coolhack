import socket
import struct
import io
from PIL import Image

sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM) 
sock.bind(("127.0.0.1",1337))
sock.listen(1)
conn,addr = sock.accept()

data = b""
while len(data) <4:
    data+=conn.recv(4)

frame_size = struct.unpuck(">L,data")[0]

while len(data) <frame_size:
    data+=conn.recv(frane_size)

frame_data=io.BytesIO(data)
frame = Image.open(frame_data)
frame.show()